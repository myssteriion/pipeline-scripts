# permet de mettre en conf les appels de fonctions afin de ne pas rebuilder l'appli en cas d'ajout de param
# les paramètres précédés de $ vont être remplacés par le code java (car ce ne sont pas des variables d'environements (d'un point de vue Pipeline))
runBuild = runBuild(env.gitRoot, env.projectRoot, env.jdkCompilation, env.mvnVersion, env.targetDirectory, env.sourceAppDirectory, env.sourceConfDirectory)
deployToSecondaryRemote = deployToSecondaryRemote(\"$secondaryRemote\")